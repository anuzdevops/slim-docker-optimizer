{
  "name": "slim-app",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && echo 'console.log(\"optimized running\")' > dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
